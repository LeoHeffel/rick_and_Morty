import styled from "styled-components";

const StyledDiv = styled.div`
color: white;
`

export default function About(props) {

   return (
      <StyledDiv>
        soy about
      </StyledDiv>
   );
}